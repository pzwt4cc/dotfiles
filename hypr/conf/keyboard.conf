# -----------------------------------------------------------

# cfg keys
input {
    kb_layout = us,ru
    kb_options = grp:alt_shift_toggle
    repeat_rate = 50
    repeat_delay = 500
}

# -----------------------------------------------------------